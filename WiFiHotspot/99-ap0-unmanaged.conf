# Keep NetworkManager (and therefore wpa_supplicant) away from ap0.
# Without this NM grabs the interface, tries to bring it up as a station,
# and fails with EBUSY -- iwlwifi allows only one managed interface.
[keyfile]
unmanaged-devices=interface-name:ap0
